package VLCS.VOCL;

import weka.core.Instances;

public class GlobalWeighting {

    public float[] getWeights(Instances chunk) {
        float[] weights = new float[chunk.size()];
        return weights;
    }
}
